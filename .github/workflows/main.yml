- name: Free disk space
  run: |
    sudo rm -rf /usr/share/dotnet
    sudo rm -rf /opt/ghc
    sudo rm -rf /usr/local/share/boost
    sudo rm -rf "$AGENT_TOOLSDIRECTORY"
    df -h  # نمایش فضای خالی بعد از پاکسازی

- name: Setup Android SDK manually with full license
  run: |
    export ANDROID_HOME=${HOME}/android-sdk
    export PATH=${ANDROID_HOME}/cmdline-tools/latest/bin:${ANDROID_HOME}/platform-tools:${PATH}
    mkdir -p ${ANDROID_HOME}/cmdline-tools
    
    # دانلود و نصب Command Line Tools
    wget -q https://dl.google.com/android/repository/commandlinetools-linux-latest.zip -O cmdline-tools.zip
    unzip -q cmdline-tools.zip -d ${ANDROID_HOME}/cmdline-tools
    mv ${ANDROID_HOME}/cmdline-tools/cmdline-tools ${ANDROID_HOME}/cmdline-tools/latest
    
    # تأیید خودکار تمام لایسنس‌ها (کلید اصلی)
    yes | ${ANDROID_HOME}/cmdline-tools/latest/bin/sdkmanager --licenses > /dev/null 2>&1
    
    # نصب حداقل مورد نیاز
    ${ANDROID_HOME}/cmdline-tools/latest/bin/sdkmanager "platform-tools" "platforms;android-33" "build-tools;33.0.0" > /dev/null 2>&1
    
    # ذخیره مسیرها برای مراحل بعدی
    echo "ANDROID_HOME=${HOME}/android-sdk" >> $GITHUB_ENV
    echo "${HOME}/android-sdk/cmdline-tools/latest/bin" >> $GITHUB_PATH
    echo "${HOME}/android-sdk/platform-tools" >> $GITHUB_PATH
    
    # بررسی نصب
    echo "Android SDK installed successfully"
